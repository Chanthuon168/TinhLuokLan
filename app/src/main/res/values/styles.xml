<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:actionMenuTextColor">@color/primary_text</item>
    </style>

    <style name="AppTheme.AppThemeNoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:actionMenuTextColor">@color/primary_text</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PopupAnimation" parent="Widget.AppCompat.PopupWindow">
        <item name="android:windowEnterAnimation">@anim/bottom_up</item>
        <item name="android:windowExitAnimation">@anim/bottom_down</item>
    </style>

    <style name="PopupAnimationReply" parent="Widget.AppCompat.PopupWindow">
        <item name="android:windowEnterAnimation">@anim/slide_in_from_left_reply</item>
        <item name="android:windowExitAnimation">@anim/slide_in_from_right_reply</item>
    </style>

    <style name="MyEditTextTheme">
        <item name="colorControlNormal">#fff</item>
    </style>

    <style name="ActionBarPopupThemeOverlay">
        <item name="colorControlNormal">@color/colorAccent</item>
    </style>

    <style name="ActionBarThemeOverlay">
        <item name="colorControlNormal">#ffffff</item>
    </style>

    <style name="HeaderTitleStyle" parent="@android:style/TextAppearance">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">20sp</item>
    </style>

</resources>
